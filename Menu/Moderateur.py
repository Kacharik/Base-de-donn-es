from Check_data import *

def Check_avis(cursor):
    '''
    good_choice = False
    while(not good_choice):
        cursor.execute("SELECT * FROM AvisValid")
        resultat = cursor.fetchall()
        print("Voici tout les Avis")
        print("-------------------")
        print()
        for ligne in resultat:
            print(ligne)
        print()
        print("Inscrivez l'id de l'avis à supprimer")
        choix = input()
        effacer_terminal()
        if(choix == 'back'):
            good_choice = True
    '''